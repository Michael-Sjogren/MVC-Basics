@model MVCBasics.ViewModels.CreateCityViewModel

<form asp-controller="City" asp-action="Create"  method="post" class="form p-5 border border-secondary" style="max-width: 28rem; margin-left: auto;">
    <h4 class="form-text">Add a new City.</h4>
    <div class="form-group">
        <label asp-for="Name"></label>
        <input class="form-control" asp-for="Name"/>
        <span class="alert-danger" asp-validation-for="Name"></span>
    </div>
    <div class="form-group">
        <label asp-for="CountryId"></label>
        <select class="form-control" asp-for="CountryId">
            @{
                foreach (var country in ViewBag.Countries)
                {
                    <option value="@country.Id">@country.Name</option>
                }
            }
        </select>
        <span class="alert-danger" asp-validation-for="CountryId"></span>
    </div>
    <button class="form-control btn btn-primary" type="submit"> Add </button>
</form>